{{- if eq .Values.nwebapi.type "internal" -}}
apiVersion: v1
kind: ConfigMap
  metadata:
  name: nserver-dashboards
data:
  mysql_by_exporter.json: |-
  {{ .Files.Get "files/mysql_by_exporter.json" | indent 4 }}
  redis_by_exporter.json: |-
  {{ .Files.Get "files/redis_by_exporter.json" | indent 4 }}
{{- end -}}
